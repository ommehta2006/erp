services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    volumes:
      - factorypulse-data:/app/data
    restart: unless-stopped
volumes:
  factorypulse-data:
